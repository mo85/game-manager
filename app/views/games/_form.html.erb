<% form_for @game, :html => { :class => :default } do |f| %>

<div>
	<%= f.label :name %>
	<%= f.text_field :name %>
</div>

<div>
	<%= f.label :description %>
	<%= f.text_area :description %>
</div>


<div>
	<%= f.label :location %>
	<%= f.select :location, Game::Positions, { :prompt => 'Bitte Auswählen...' } %>
</div>

<div>
	<%= f.submit label %>
</div>

<% end %>
